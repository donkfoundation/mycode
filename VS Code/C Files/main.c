#include <stdio.h>

int main(void){ 
    float average = 2.50; // Number with decimals "%f"
    char jaj = 'A'; // Single character "%c"
    char name[] = "Deamon"; // Array (string) "%s"
    int hola = 12; // Integer, complete number "%d"
    printf("%c", jaj);
    return 0;
}